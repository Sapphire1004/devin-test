# QR 스탬프 랠리 웹앱 — 모델 비교 계획

## 1. 목적

GPT Astra와 Claude Fable 5.1에 동일한 요구사항을 전달하고, QR을 스캔해 방문 스탬프를 등록하는 웹앱의 완성도와 사용성을 비교한다.
모델 이름은 실제 실행 환경에서 선택하고 기록한다. 경로 이름만으로 사용 모델을 판단하지 않는다.

## 2. 공통 실행 조건

| 항목 | 조건 |
| --- | --- |
| 저장소 | `https://github.com/Sapphire1004/devin-test` |
| 필수 스택 | Vite, React, TypeScript **5.0.4** (`^` 없이 고정) |
| Astra 구현 | `http://localhost:3000/astra` |
| Claude 구현 | `http://localhost:3000/claude` |
| 실행 방식 | 하나의 Vite 서버, `--host 0.0.0.0 --port 3000 --strictPort` |
| 공통 버전 | 시작 전에 Node·npm·Vite·React 버전을 공통 베이스에서 확정하고 잠금 파일로 고정 |
| 자유 선택 | 디자인, UI·CSS·QR 라이브러리, 상태 관리, 폴더 내부 구조 |
| 언어·화면 | 한국어 UI, 데스크톱과 모바일 반응형 |

두 모델은 동일한 초기 커밋과 이 문서로 시작한다. 서로의 구현을 보지 않고 독립적으로 작업한다.
작업 시간 한도·도구 접근·수정 기회도 같게 주고, 실행한 모델명과 소요 시간을 기록한다.

## 3. 필수 사용자 흐름

1. **참여:** 랠리 소개와 5개 장소를 보고 ‘참여하기’를 눌러 스탬프 카드를 발급받는다.
2. **등록:** 카메라로 장소의 QR을 스캔하면 해당 장소의 스탬프가 등록된다.
3. **대체 입력:** 카메라가 없는 환경에서는 QR 이미지 업로드로 같은 동작을 수행한다.
4. **진행 확인:** 획득한 장소, 방문 시각, 진행도 `0/5`~`5/5`를 확인한다.
5. **완주:** 5개 스탬프를 모두 모으면 완주 상태가 표시된다.
6. **재방문:** 새로고침 후에도 참여 여부와 스탬프가 유지된다.
7. **다시 테스트:** 확인 절차가 있는 ‘기록 초기화’로 자신의 구현 데이터만 삭제한다.

같은 장소의 QR을 다시 스캔해도 스탬프 수와 최초 방문 시각이 바뀌지 않아야 한다.
잘못된 QR, 다른 행사 QR, 카메라 권한 거부·장치 없음, 판독 실패는 안내하고 재시도할 수 있어야 한다.
스캔 화면을 닫거나 다른 구현으로 이동하면 카메라 사용을 종료한다.

## 4. 동일한 테스트 데이터

행사명은 **우리 동네 스탬프 랠리**로 한다. 아래 문자열 전체가 QR의 내용이며 정확히 일치할 때만 인정한다.

| 장소 | QR 내용 |
| --- | --- |
| 시작 광장 | `stamprally:v1:spot-1` |
| 동네 책방 | `stamprally:v1:spot-2` |
| 골목 카페 | `stamprally:v1:spot-3` |
| 작은 공원 | `stamprally:v1:spot-4` |
| 전망대 | `stamprally:v1:spot-5` |

두 구현에 동일한 QR 이미지 5장을 제공한다. QR 문자열이나 버튼으로 스탬프를 직접 추가하는 기능은 실제 스캔 성공으로 평가하지 않는다.
잘못된 입력은 `hello`, 미등록 장소는 `stamprally:v1:spot-99`, 다른 행사는 `other-rally:v1:spot-1`을 사용한다.
인증·서버·DB 없이 브라우저 로컬 저장소로 구현하는 검증용 앱이며, 실제 방문 증명이나 부정 등록 방지는 이번 범위에 포함하지 않는다.

## 5. 한 프리뷰에서 구현 분리

- 공통 진입점에서 `/astra`는 `src/astra/App.tsx`, `/claude`는 `src/claude/App.tsx`의 default export를 로드한다.
- 각 모델은 자신의 폴더에서 앱을 구현한다. 스타일은 CSS Modules 또는 경로별 최상위 컨테이너로 격리한다.
- 저장 키는 각각 `stamprally:astra:v1`, `stamprally:claude:v1`을 사용한다. 첫 실행은 모두 미참여·스탬프 0개다.
- 필수 스택 버전을 바꾸지 않는 범위에서 의존성을 자유롭게 추가한다. 최종 통합 시 공통 잠금 파일을 패키지 매니저로 재생성한다.
- 루트 `/`에는 두 구현으로 이동하는 비교용 링크만 둔다. 각 경로 직접 접속과 새로고침도 지원한다.
- 별도 브랜치에서 구현한 결과를 통합한 뒤, 같은 서버·브라우저·장치·권한·화면 크기에서 비교한다.

카메라는 localhost 또는 HTTPS에서 확인한다. Devin 프리뷰에서는 동일한 프리뷰 주소의 두 경로를 사용하고,
iframe의 카메라 권한이 제한되면 양쪽 모두 새 탭에서 동일 조건으로 재시험한다. 사용자는 호스트 컴퓨터의 localhost로 접속하지 않는다.

## 6. 비교 절차

1. 두 경로의 기록을 각각 초기화하고 데스크톱 `1440×900`, 모바일 `390×844`에서 화면을 확인한다.
2. 참여 → QR 1개 등록 → 동일 QR 재등록 → 잘못된 QR → 새로고침 순서로 검사한다.
3. 나머지 4개 QR로 완주하고, 한 구현의 기록이 다른 구현에 반영되지 않는지 확인한다.
4. 카메라 거부 후 이미지 업로드, 스캐너 종료 후 카메라 해제, 개별 기록 초기화를 확인한다.
5. 각 구현에서 lint·타입 검사·빌드를 통과했는지 확인한다. 실제 카메라를 시험하지 못한 경우 ‘미검증’으로 남긴다.

평가는 **기능 정확성 40 / 디자인·사용성 30 / 모바일·접근성 20 / 오류 처리 10**으로 한다.
키보드 조작, 버튼·입력의 이름, 읽기 쉬운 대비도 확인한다. 점수에는 양쪽에 동일한 테스트 증거를 첨부한다.

## 7. 두 모델에 전달할 공통 프롬프트

> PLAN.md를 읽고 QR 스탬프 랠리 웹앱을 구현하세요.
> 필수 스택은 Vite, React, TypeScript 5.0.4이며 나머지 기술과 디자인은 자유입니다.
> 배정된 경로·폴더만 담당하고 상대 구현은 열람하거나 수정하지 마세요.
> 문서의 필수 흐름, 공통 QR 데이터, 저장·스타일 격리 조건을 지키세요.
> lint·타입 검사·빌드를 실행하고, 실행 방법·추가 의존성·검증 결과·미검증 항목을 보고하세요.

이 프롬프트 뒤에 배정 정보만 다르게 붙인다.

- GPT Astra: `담당 경로 /astra, 담당 폴더 src/astra`
- Claude Fable 5.1: `담당 경로 /claude, 담당 폴더 src/claude`
